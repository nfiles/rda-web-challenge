(function ($) {
    $(function (argument) {
        var tab_window = $('.tab-window');

        tab_window.tabbify({
            // speed: 50,
            // initial_tab: 3
        });

    });
})( jQuery );
